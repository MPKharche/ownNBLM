# Production — Vercel rewrites proxy /api to VPS (see vercel.app.json when live).
# FRONTEND_URL=https://frontend-jet-ten-16.vercel.app
# VITE_API_URL=   # leave empty when using Vercel rewrites to 195.35.6.159:8010
